[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    frozen: tests that require PyInstaller build (run manually after build)
    slow: integration tests that load real models (>10s)
